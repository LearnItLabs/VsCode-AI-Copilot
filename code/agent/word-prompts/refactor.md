## Prompt 1

Split this HTML page into index.html, styles.css, and app.js. 
Remove inline CSS/JS and inline event handlers

## Prompt 2
Refactor this monolithic HTML page to improve separation of concerns.

Goal: move all CSS into an external stylesheet and all JavaScript into an external script file, while keeping behavior and appearance unchanged

## Prompt 3
 I want to separate concerns in this HTML file. Propose a refactor plan to extract CSS into styles.css and JS into app.js.
Before applying any large changes across the file, show me the plan and the list of concrete edits you’ll make. Then proceed.