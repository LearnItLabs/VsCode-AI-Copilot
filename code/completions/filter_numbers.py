  # version 1
	# write a function that filters a list of integers
	# by a given threshold and returns the filtered list.


	# version 2
	# define a function that takes a list of integers and a threshold value,
	# and returns a new list containing only the integers greater than the threshold.

	

  # return a list of unique items, filtered to only even numbers,
  # sorted in ascending order


 

#  Given a list of numbers, 
#  return a dictionary containing the minimum, maximum, average, and count.
